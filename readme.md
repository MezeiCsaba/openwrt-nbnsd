# openwrt-nbnsd

A lightweight NetBIOS Name Service (NBNS) daemon for OpenWrt.

## Goal

Provide just enough NBNS functionality for legacy SMB clients
(IP cameras, embedded devices, etc.) to discover a `ksmbd`
server without requiring Samba4.

## Current status

Project bootstrap.